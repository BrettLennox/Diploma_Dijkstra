using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WhatHappens : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(float.MaxValue + 100f);
    }
}
